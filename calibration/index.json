{
  "version": 1,
  "axes": {
    "model": { "default": "claude-5", "files": { "claude-5": "calibration/claude-5.md" } },
    "harness": { "default": "claude-code", "files": { "claude-code": "calibration/harness-claude-code.md" } }
  },
  "contract": {
    "value_requires": ["value", "source"],
    "unset_policy": "1차 소스 인용이 없는 값은 unset으로 남는다. 스킬은 추측하면 안 된다. calibrated_by가 unset으로 풀리는 항목은 그 항목의 on_unset 필드가 처리한다 (drop | ask).",
    "adding_a_generation": "축마다 파일을 하나 더하고 default를 그것으로 가리킨다. 스킬 본문은 바뀌지 않는다."
  }
}

# 빌드 산출물과 Astro 캐시. 루트 .gitignore 는 node_modules/ 만 잡는다.
# 여기 두는 이유는 무시 규칙이 그것이 생기는 디렉터리 옆에 살아야 site/ 를 떼어내도
# 같이 따라가기 때문이다.
dist/
.astro/

# 공유 카드 이미지. scripts/og-image.mjs 가 custom.css 의 색을 읽어 빌드 전에 만든다.
# 커밋하면 강조색을 바꿨을 때 PNG만 옛 색으로 남고, 그 사실은 공유해 보기 전에는 모른다.
public/og.png

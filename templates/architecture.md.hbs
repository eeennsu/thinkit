# Architecture

The dependency direction is enforced by `boundaries/dependencies` in
`eslint.config.mjs`, generated from the layer graph. This file exists for the
part the linter cannot hold: where the repo currently violates its own rule.

## Exceptions

{{exceptions}}

New code does not inherit these. They are recorded so that reading the
existing code does not teach the wrong convention.

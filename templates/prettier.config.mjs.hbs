// 고정값. 포매팅 취향은 잘못 정해도 값이 안 들고 열어두면 논쟁 값이 드므로, 도구가
// 정하고 아무도 적어두지 않는다.
//
// importOrder는 아키텍처 모듈의 레이어 그래프에서 생성된다. 손으로 고치지 않는다.
// 그래프에는 소유자가 하나이고, 이 목록은 그 그래프를 모든 파일 맨 위에 보이게
// 만든 것이다. 경계를 강제하지 않는 모듈에서는 소스 루트 하나만 자기 그룹을 받는다.
// 부수효과 import는 있던 자리에 둔다 — 플러그인이 그것들을 장벽으로 다루므로 여기서
// 그렇게 말할 필요가 없다.
// 다시 생성하려면:  /harness-bootstrap {{stack}}
export default {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
  plugins: ['@ianvs/prettier-plugin-sort-imports'],
  importOrder: {{importOrderJs}},
};

# {{projectName}}

{{oneLine}}
{{safetySection}}{{referencesSection}}
## 함정

<!--
  코드를 읽고 나서도 남는 사실. 이유가 레포 안에 없는 결정, 버그처럼 보이는 함정,
  아무것도 검사하지 않는 불변식.

  새 레포에서는 비어 있는 것이 올바른 상태다. 추측으로 채우지 않는다.
  틀린 함정은 없는 것보다 나쁘다. 부딪힐 때마다 항목을 더한다.

  레이어 경계, 슬라이스 격리, public API 접근은 eslint(`npm run lint`)가 강제하므로
  일부러 여기 다시 적지 않는다.
-->

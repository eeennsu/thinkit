---
name: verification
description: 이 레포의 변경을 검증할 때 쓴다. 레포를 아는 것이 필요하고, 테스트를 돌리는 것만으로는 부족한 경우.
---

# 검증

이 레포가 알고 모델은 읽어서 알아낼 수 없는 것:

{{verificationFacts}}

<!--
  인터뷰 Q3에서 생성됐다. 여기 있는 것은 전부 답변에서 왔다.

  "변경을 파악하고, 가장 작은 테스트 집합을 돌리고, 무엇을 돌렸는지 보고하라" 같은
  절차는 일부러 없다. 모델은 시키지 않아도 이미 그렇게 하고, 적어두면 더해지는 게
  아니라 자기 행동과 겹쳐서 누적된다. 이 파일이 그런 단계만 담게 되면 파일을 지운다.
-->

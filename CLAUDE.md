# thinkit

레포의 하네스 세팅(CLAUDE.md / Skills / References)을 자동화하는 플러그인. 세팅을 **대신 써주지
않고**, 레포 소유자가 답해야 정해지는 것들을 물어서 그 답으로 구성한다.

판정 기준은 세 층이다. 모델 세대와 무관한 것은 `principles/`, 세대에 따라 위치가 바뀌는 값은
`calibration/`, 레포마다 다르게 답하는 경계 규약은 `modules/`에 있다. 현재 어느 세대를 기준으로
도는지는 `calibration/index.json`이 가리키고, 새 세대는 파일 하나를 더하는 것으로 대응한다.

## 참조

- 컨텍스트 엔지니어링 — [docs/canon.md](docs/canon.md)
- 하네스 파일 배치 패턴 — [docs/structure-patterns.md](docs/structure-patterns.md)
- 무엇을 왜 그렇게 정했는지 — [docs/design-log.md](docs/design-log.md)
- 원문 아카이브 — [docs/references/](docs/references/)

## 어기면 안 되는 것

**내용이 아니라 질문 세트를 배포한다.**
플러그인이 실어 나르는 것은 완성된 규칙 문장이 아니라, 그 규칙을 정하기 위한 질문이다.
남의 레포에 맞는 답을 우리가 알 수 없다.

**아키텍처는 슬롯이고, 슬롯에는 빠져나갈 답이 있다.**
경계 규약은 `modules/<name>/`이 소유하고 레포가 인터뷰에서 고른다. 스택 프로필이 지목하는
모듈은 확인받으러 들고 가는 제안이지 답이 아니다. 경계를 강제하지 않는 답이 항상 있어야 하고,
그것을 고른 레포도 나머지 하네스를 전부 받는다. 이 조항이 없으면 아키텍처는 우리가 묻지 않고
심는 유일한 답이 되고, 그것이 곧 첫 번째 조항 위반이다.

**repo-visible 정보를 생성하지 않는다.**
디렉터리 구조, 파일 목록, 언어·프레임워크처럼 파일 시스템을 보면 알 수 있는 것은
어떤 산출물에도 쓰지 않는다. 예외는 README에 한해, 설치를 결정하기 전에는 알 수 없는
독자를 향한 서술이다 — `ls` 한 번이면 아는 것과, 설치 전에는 알 수 없는 것은 같은
문장이라도 값이 다르다.

**Gotcha 섹션은 비워둔 채 출고한다.**
gotcha는 정의상 레포 안에서만 알 수 있다. 추측으로 채운 gotcha는 틀린 gotcha고,
틀린 gotcha는 없는 것보다 나쁘다. 빈 섹션과 질문만 남긴다. 예외는 레포 소유자가 직접
입력한 문자열이다 — 추측이 아니므로 요약하거나 새로 쓰지 않고 받아 적는다.

**도구로 강제 가능한 건 문서에서 뺀다.**
린터·포매터·훅·CI·타입체커·권한 설정으로 막을 수 있는 규칙은 문서에 쓰지 않고 그 도구에 넣는다.
문서에 남은 규칙은 도구가 강제할 수 없는 것만이어야 한다.

**취향은 규칙이 되지 않는다.**
이 플러그인은 코드 취향을 소유하지 않는다. 코드를 지적하는 규칙은 "모델이 안 그러면 내보낼
버그를 막는가"에 답해야 하고, 답하지 못하는 규칙은 문서에서도 린트 설정에서도 뺀다. 도구에
넣을 수 있다는 것이 넣어도 된다는 뜻은 아니다. 포매터는 지적하지 않고 고쳐 쓰므로 이 조항이
말하는 규칙이 아니다.

**세대 의존 값을 문서에 굳히지 않는다.**
모델 이름과 세대별 기본값은 `calibration/`에만 산다. 스킬·스크립트·산출물은 물론 판정 기준의
질문 문자열과 루브릭 산문도 그 값을 참조할 뿐 문장으로 박아넣지 않는다. 인용할 1차 소스가
없는 값은 숫자를 지어내지 말고 `unset`으로 둔다.
